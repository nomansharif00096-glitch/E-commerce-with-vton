import 'package:flutter/material.dart';

class Palette {
  // static const mainColor = Color(0xff128F46);
  static const mainColorLightMode = Color.fromRGBO(244, 131, 11, 1);
  static const mainColordarkMode = Color.fromRGBO(124, 77, 255, 1);
  static const whiteColor = Colors.white;
  static const blackColor = Colors.black87;
  static const itemCardLightMode = Colors.white;
  static const itemCardDarkMode = Color.fromRGBO(55, 55, 79, 1);
}
